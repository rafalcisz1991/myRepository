SELECT U.ISSUESLIST_ID, P.NAME
FROM ISSUES U, ISSUESLISTS P
JOIN ISSUES I on P.ID = I.ISSUESLIST_ID;

SELECT U.ISSUESLIST_ID, P.ID
FROM ISSUES U, users P
JOIN ISSUES I on P.ID = I.USER_ID_ASSIGNEDTO;

SELECT FIRSTNAME, LASTNAME, COUNT(*) AS USER_ID_ASSIGNEDTO
FROM users, ISSUES