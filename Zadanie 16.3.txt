CREATE TABLE ISSUESLISTS
(
    ID SERIAL PRIMARY KEY,
    NAME Varchar(100)
);

CREATE TABLE ISSUES
(
    ID SERIAL PRIMARY KEY,
    ISSUESLIST_ID  BIGINT UNSIGNED NOT NULL,
    FOREIGN KEY (ISSUESLIST_ID) REFERENCES issueslists(ID),
    SUMMARY VARCHAR(100),
    DESCRITPION VARCHAR(100),
    USER_ID_ASSIGNEDTO BIGINT UNSIGNED NOT NULL,
    FOREIGN KEY (USER_ID_ASSIGNEDTO) references users(id)
);

INSERT INTO issueslists (NAME)
value ('ToDo');

INSERT INTO issueslists (NAME)
value ('In progress');

INSERT INTO issueslists (NAME)
value ('Done');

COMMIT;

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRITPION, USER_ID_ASSIGNEDTO)
VALUES (1, 'podsumowanie To Do', 'opis To Do', 1);

COMMIT;

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRITPION, USER_ID_ASSIGNEDTO)
VALUES (2, 'podsumowanie In progress', 'opis In progress', 1);

INSERT INTO ISSUES (ISSUESLIST_ID, SUMMARY, DESCRITPION, USER_ID_ASSIGNEDTO)
VALUES (3, 'podsumowanie Done', 'opis Done', 1);

COMMIT;
